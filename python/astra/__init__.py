"""Astra — from-scratch AI research project.

Phase 0 provides a NumPy reference implementation of the full pipeline:
byte-level tokenizer, Transformer core, training loop, evaluation harness,
and safety gates (data filters + contamination checks).

Importable as `astra.*`. See docs/ for the master specification.
"""

__version__ = "0.0.1"